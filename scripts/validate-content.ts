/**
 * Placeholder for JSON-schema validation of content/*.json ahead of Phase 1's full
 * rulebook transcription (weapons, armor, talents, spells, lifepath tables, etc.).
 * Intended to run in CI once real schemas are defined, mirroring the validation step
 * in CompCon's content-authoring tooling.
 */
async function main() {
  console.log('validate-content: no schemas defined yet — nothing to validate.')
}

main()
